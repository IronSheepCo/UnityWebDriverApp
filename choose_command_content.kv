#:kivy 1.0

#:set height 30

ScrollView:
    StackLayout:
        size_hint: 1, None
        height: self.minimum_height
        Button:
            size_hint:1,None
            text:"Click"
            height:height
            on_release:root.choose(1)
        Button:
            size_hint:1,None
            text:"Get text"
            height:height
            on_release:root.choose(2)
        Button:
            size_hint:1,None
            text:"Get attribute"
            height:height
            on_release:root.choose(3)
        Button:
            size_hint:1,None
            text:"Get name"
            height:height
            on_release:root.choose(4)
        Button:
            size_hint:1,None
            text:"Is visible"
            height:height
            on_release:root.choose(9)
        Button:
            size_hint:1,None
            text:"Wait for element"
            height:height
            on_release:root.choose(6)
        Button:
            size_hint:1,None
            text:"Wait and Click"
            height:height
            on_release:root.choose(7)
        Button:
            size_hint:1,None
            text:"Wait"
            height:height
            on_release:root.choose(5)
        Button:
            size_hint:1,None
            text:"Wait for Visible"
            height:height
            on_release:root.choose(11)
        Button:
            size_hint:1,None
            text:"Wait and get text"
            height:height
            on_release:root.choose(8)
        Button:
            size_hint:1,None
            text:"Wait and get name"
            height:height
            on_release:root.choose(10)
        
