#:kivy 1.0

#:set height 30

ConnectEntry:
    height:height
    size_hint:1, None

    target_ip:target_ip
    target_port:target_port
    target_name:target_name
    target_device_id:target_device_id
    connect_button:connect_button

    BoxLayout:
        orientation:"horizontal"
        size_hint:1,1

        Label: 
            text:"Device Ip: "
            height:height
            size_hint:0.05, None
        Label:
            id: target_ip
            text:""
            height:height
            size_hint: 0.1, None
        Label:
            text:"Port: "
            height:height
            size_hint:0.05, None
        Label:
            id: target_port
            text:""
            height: height
            size_hint: 0.05, None
        Label:
            text:"Name: "
            height:height
            size_hint:0.05, None
        Label:
            id: target_name
            text:""
            height: height
            size_hint: 0.15, None
        Label:
            text:"ID: "
            height:height
            size_hint:0.05, None
        Label:
            id: target_device_id
            text:""
            height: height
            size_hint: 0.35, None
        Button:
            id:connect_button
            size_hint:0.15, 1
            text:"Connect"
            on_press:root.connect_to_server_callback(self)